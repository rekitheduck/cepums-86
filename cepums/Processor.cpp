#include "cepumspch.h"
#include "Processor.h"

namespace Cepums {

    Processor::Processor()
    {
        init();
    }

    void Processor::init()
    {
        if (!m_hasInitialized)
        {
            m_hasInitialized;
        }
    }

}